# 未実装機能レポート (2026-02-13)

## 📊 概要
LinkUpプラットフォームで未実装またはスタブのみの機能をリストアップしました。

---

## 1️⃣ イベントオーガナイザー管理画面 - 未実装機能

### 🎯 マーケティング機能 (Marketing)
**場所**: `renderOrganizerMarketing()` 関数 (index.html 6391行目〜)

#### ❌ 未実装項目:

##### 1. **メッセージ配信機能**
- **説明**: フォロワーや過去の参加者に一斉メッセージ送信
- **UI**: "新規メッセージ作成 (10円/通)" ボタンあり
- **状態**: ボタンに`onclick`ハンドラーなし（クリック不可）
- **必要な実装**:
  - メッセージ作成モーダル
  - 送信先選択機能（フォロワー、過去参加者、セグメント別）
  - 一斉送信API（`POST /api/messages/bulk`）
  - 送信履歴管理
  - 課金処理（10円/通）

##### 2. **クーポン管理機能**
- **説明**: 割引クーポンの発行と管理
- **UI**: "クーポン発行" ボタンあり
- **状態**: ボタンに`onclick`ハンドラーなし（クリック不可）
- **バックエンド**: `campaigns.ts`に実装済み（`POST /api/campaigns`, `POST /api/campaigns/verify`）
- **必要な実装**:
  - クーポン作成モーダル（割引率、有効期限、使用条件）
  - バックエンドAPIとの接続（`API.Campaigns.create()`）
  - クーポン一覧表示
  - クーポン編集・削除機能
  - 使用状況統計

##### 3. **おすすめ枠への掲載機能**
- **説明**: トップページの「おすすめ」枠にイベントを掲載
- **UI**: "掲載申請 (5,000円/週)" ボタンあり
- **状態**: ボタンに`onclick`ハンドラーなし（クリック不可）
- **必要な実装**:
  - 掲載申請モーダル（イベント選択、掲載期間選択）
  - 決済処理（5,000円/週）
  - 管理者承認フロー
  - 掲載状況確認画面

---

## 2️⃣ 総管理者画面 - 未実装機能

### 💾 バックアップ管理機能 (Backups)
**場所**: `renderAdminBackups()` 関数 (index.html 10938行目〜)

#### ❌ 未実装項目:

##### 1. **自動バックアップスケジュール設定**
- **説明**: 定期バックアップのスケジュール設定
- **UI**: "自動バックアップ設定" ボタンあり
- **状態**: スタブ実装（`showToast('自動バックアップスケジュール設定画面を開きます', 'info')`のみ）
- **必要な実装**:
  ```javascript
  function scheduleBackup() {
      // スケジュール設定モーダル表示
      // - バックアップ頻度（毎日、毎週、毎月）
      // - バックアップ時刻
      // - 保持期間
      // API: POST /api/admin/backups/schedule
  }
  ```

##### 2. **全データバックアップ作成**
- **説明**: システム全体のバックアップを手動作成
- **UI**: "全データバックアップ" ボタンあり
- **状態**: スタブ実装（確認ダイアログのみ、実際のバックアップ処理なし）
- **必要な実装**:
  ```javascript
  async function createFullBackup() {
      // 確認ダイアログ
      // バックアップ処理開始
      // API: POST /api/admin/backups/full
      // 進捗表示
      // 完了通知
  }
  ```

##### 3. **個別データバックアップ**
- **説明**: ユーザー、イベント、決済などのデータを個別にバックアップ
- **UI**: 各データカードに "バックアップ" ボタンあり
- **状態**: スタブ実装（`showToast`のみ）
- **必要な実装**:
  ```javascript
  async function createBackup(backupType) {
      // backupType: 'users', 'events', 'payments', etc.
      // API: POST /api/admin/backups/create
      // パラメータ: { type: backupType }
  }
  ```

##### 4. **バックアップ復元機能**
- **説明**: バックアップからデータを復元
- **UI**: 各データカードに "復元" ボタンあり
- **状態**: スタブ実装（`showToast`のみ）
- **必要な実装**:
  ```javascript
  async function restoreBackup(backupType) {
      // 確認ダイアログ（危険な操作）
      // バックアップ選択（日時リスト）
      // API: POST /api/admin/backups/restore
      // パラメータ: { type: backupType, backup_id: selectedId }
  }
  ```

---

## 📋 実装優先度

### 🔥 高優先度
1. **クーポン管理機能** - バックエンド実装済み、フロントエンド接続のみ必要
2. **バックアップ作成・復元** - 本番運用に必須

### 🟡 中優先度
3. **メッセージ配信機能** - マーケティング機能として有用
4. **おすすめ枠掲載** - 収益化機能

### 🔵 低優先度
5. **自動バックアップスケジュール** - Cloudflareの機能で代替可能

---

## 🛠️ 技術的詳細

### バックエンドAPI実装状況

#### ✅ 実装済み
- `POST /api/campaigns` - キャンペーン作成
- `POST /api/campaigns/verify` - クーポンコード検証
- `GET /api/campaigns` - キャンペーン一覧取得

#### ❌ 未実装
- `POST /api/messages/bulk` - 一斉メッセージ送信
- `POST /api/admin/featured-listings` - おすすめ枠掲載申請
- `POST /api/admin/backups/schedule` - 自動バックアップ設定
- `POST /api/admin/backups/full` - 全データバックアップ
- `POST /api/admin/backups/create` - 個別バックアップ
- `POST /api/admin/backups/restore` - バックアップ復元
- `GET /api/admin/backups` - バックアップ一覧取得

### フロントエンドAPI接続

#### ❌ 未実装
```javascript
// index.html内のAPIクライアント拡張が必要
const API = {
    // 既存...
    Campaigns: {
        create: async (data) => { /* ... */ },
        list: async () => { /* ... */ },
        verify: async (promoCode) => { /* ... */ }
    },
    Messages: {
        sendBulk: async (data) => { /* ... */ }
    },
    Admin: {
        // 既存...
        Backups: {
            schedule: async (config) => { /* ... */ },
            createFull: async () => { /* ... */ },
            create: async (type) => { /* ... */ },
            restore: async (type, backupId) => { /* ... */ },
            list: async () => { /* ... */ }
        }
    }
};
```

---

## ✅ 実装済み機能（参考）

### イベントオーガナイザー管理画面
- ✅ ダッシュボード（売上統計）
- ✅ イベント管理（作成、編集、削除）
- ✅ イベント承認申請フロー
- ✅ 資料アップロード・ダウンロード
- ✅ 運営・受付（ユーザー検索、入場受付、参加者リスト、座席管理）
- ✅ 分析（チケット販売統計、締め切り管理）
- ✅ 会場管理
- ✅ 入出金管理
- ✅ プロフィール設定

### 総管理者画面
- ✅ ダッシュボード
- ✅ 主催者管理
- ✅ ユーザー管理
- ✅ イベント承認（承認/却下）
- ✅ 会場管理
- ✅ チケット管理
- ✅ チケット検証
- ✅ 入出金履歴
- ✅ 財務管理
- ✅ アクセスログ
- ✅ サーバーステータス
- ✅ エラーログ
- ✅ eKYC管理
- ✅ 管理者スタッフ管理
- ✅ AI設定
- ✅ バージョン管理
- ✅ 広告管理
- ✅ お知らせ管理
- ✅ ブログ管理
- ✅ サイトマップ
- ✅ サポートチャット
- ✅ デモデータ管理
- ✅ システム設定

---

## 📝 まとめ

### 未実装機能数
- **イベントオーガナイザー管理画面**: 3機能
  - メッセージ配信
  - クーポン管理（バックエンド実装済み）
  - おすすめ枠掲載
  
- **総管理者画面**: 4機能
  - 自動バックアップスケジュール
  - 全データバックアップ
  - 個別データバックアップ
  - バックアップ復元

### 実装工数見積もり

| 機能 | 優先度 | フロントエンド | バックエンド | 合計工数 |
|-----|-------|-------------|------------|---------|
| クーポン管理 | 高 | 4時間 | 0時間（実装済み） | 4時間 |
| バックアップ作成 | 高 | 3時間 | 8時間 | 11時間 |
| バックアップ復元 | 高 | 3時間 | 6時間 | 9時間 |
| メッセージ配信 | 中 | 6時間 | 8時間 | 14時間 |
| おすすめ枠掲載 | 中 | 4時間 | 6時間 | 10時間 |
| 自動バックアップ | 低 | 2時間 | 4時間 | 6時間 |
| **合計** | - | **22時間** | **32時間** | **54時間** |

---

**生成日時**: 2026-02-13 15:15 JST  
**バージョン**: v3.9.0-APPROVAL-COMPLETE  
**作成者**: Claude Code Assistant
