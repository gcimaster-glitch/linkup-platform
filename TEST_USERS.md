# 🔐 テストユーザーアカウント情報

## 📋 テストユーザー一覧

LinkUpプラットフォームのテスト用に3つのユーザーアカウントを用意しています。
各アカウントは異なる権限レベルを持ち、それぞれの機能をテストできます。

---

### 1️⃣ 管理者 (Admin)

#### ログイン情報
- **Email**: `admin@linkup.live`
- **Password**: `Admin@2026!`
- **Role**: `admin`
- **KYC Status**: `verified`

#### アクセス可能な機能
✅ **全機能にアクセス可能**

- 🎯 **管理者コンソール**
  - ユーザー管理（追加・編集・削除・権限変更）
  - イベント承認・却下
  - 主催者管理
  - チケット管理・検証
  - 決済履歴確認
  - 財務管理
  - システム設定
  - バックアップ管理
  - アクセスログ・エラーログ閲覧
  - eKYC管理
  - 広告・お知らせ・ブログ管理

- 📈 **イベント主催者機能（adminはorganizerの機能も使用可能）**
  - イベント作成・編集・削除
  - チケット管理
  - 参加者管理
  - 売上・分析ダッシュボード
  - マーケティング機能

- 👤 **一般ユーザー機能**
  - イベント検索・閲覧
  - チケット購入
  - マイページ

#### UI表示
- ヘッダーに「管理者」ボタン表示（赤色）
- ヘッダーに「主催者」ボタン表示（青色）
- ヘッダーに「イベント作成」ボタン表示（オレンジ色）

---

### 2️⃣ イベント主催者 (Organizer)

#### ログイン情報
- **Email**: `organizer@linkup.live`
- **Password**: `Organizer@2026!`
- **Role**: `organizer`
- **KYC Status**: `verified`

#### アクセス可能な機能
✅ **イベント作成・管理機能**

- 📈 **主催者ダッシュボード**
  - イベント作成・編集・削除
  - イベント承認申請（管理者承認待ち）
  - チケット管理（種類・価格・在庫）
  - 参加者管理
  - 入場受付・チケット検証
  - 座席管理
  - 売上統計・分析
  - 締め切り管理
  - 会場管理

- 💼 **運営機能**
  - ユーザー検索
  - 入出金管理
  - 決済履歴確認

- 🎨 **マーケティング機能**
  - メッセージ配信（未実装）
  - クーポン管理（未実装）
  - おすすめ枠掲載申請（未実装）

- 👤 **一般ユーザー機能**
  - イベント検索・閲覧
  - チケット購入
  - マイページ

#### UI表示
- ヘッダーに「主催者」ボタン表示（青色）
- ヘッダーに「イベント作成」ボタン表示（オレンジ色）
- 管理者ボタンは**表示されない**

---

### 3️⃣ 一般ユーザー (User)

#### ログイン情報
- **Email**: `user@linkup.live`
- **Password**: `User@2026!`
- **Role**: `user`
- **KYC Status**: `verified`

#### アクセス可能な機能
✅ **イベント参加機能のみ**

- 🎫 **イベント参加**
  - イベント検索・閲覧
  - カテゴリー別一覧
  - イベント詳細確認
  - チケット購入
  - 座席選択（指定席イベント）
  - 決済

- 👤 **マイページ**
  - プロフィール編集
  - 購入チケット一覧
  - 参加予定イベント一覧
  - 決済履歴
  - お気に入りイベント
  - eKYC 本人確認
  - サポート・お問い合わせ

#### UI表示
- ヘッダーに**ボタンなし**（ログイン/ログアウトのみ）
- 主催者ボタンは**表示されない**
- 管理者ボタンは**表示されない**

---

### 4️⃣ 既存ユーザー（一般ユーザーに変更）

#### ログイン情報
- **Email**: `iwama@inre.co.jp`
- **Password**: （既存のパスワード）
- **Role**: `user`（マイグレーションで変更）
- **KYC Status**: `verified`

#### 変更内容
以前は権限が混在していましたが、マイグレーション後は**一般ユーザー**として明確に定義されました。

---

## 🔒 セキュリティ機能

### 1. 権限チェック（フロントエンド）
```javascript
// オーガナイザー画面
if (!store.user || (store.user.role !== 'organizer' && store.user.role !== 'admin')) {
    showToast('イベント主催者または管理者の権限が必要です', 'error');
    navigate('home');
    return;
}

// 管理者画面
if (!store.user || store.user.role !== 'admin') {
    showToast('管理者権限が必要です', 'error');
    navigate('home');
    return;
}
```

### 2. 権限チェック（バックエンド）
```typescript
// adminMiddleware
if (role !== 'admin') {
    return c.json({ error: 'Forbidden: Admin role required' }, 403);
}

// organizerMiddleware
if (role !== 'organizer' && role !== 'admin') {
    return c.json({ error: 'Forbidden: Organizer or Admin role required' }, 403);
}
```

### 3. UI表示制御
```javascript
// 管理者ボタン（admin のみ）
if (store.user && store.user.role === 'admin') {
    adminBtn.style.display = 'flex';
} else {
    adminBtn.style.display = 'none';
}

// 主催者ボタン（organizer または admin）
if (store.user && (store.user.role === 'organizer' || store.user.role === 'admin')) {
    organizerBtn.style.display = 'flex';
} else {
    organizerBtn.style.display = 'none';
}
```

---

## 🚀 データベース登録手順

### 方法1: Cloudflare Dashboard（推奨）

1. https://dash.cloudflare.com/ にアクセス
2. D1 Database → `linkup-db` を選択
3. Console タブを開く
4. `database/migrations/0007_create_test_users.sql` の内容をコピー
5. コンソールに貼り付けて実行
6. 確認クエリで4ユーザーが登録されたことを確認

### 方法2: Wrangler CLI

```bash
cd /home/user/webapp
npx wrangler d1 execute linkup-db --file=database/migrations/0007_create_test_users.sql --remote
```

---

## 📝 テスト手順

### 1. 管理者テスト
1. `admin@linkup.live` / `Admin@2026!` でログイン
2. ヘッダーに「管理者」「主催者」「イベント作成」ボタンが表示されることを確認
3. 「管理者」ボタンをクリック → 管理者コンソールにアクセスできることを確認
4. イベント承認・却下機能をテスト
5. ログアウト

### 2. オーガナイザーテスト
1. `organizer@linkup.live` / `Organizer@2026!` でログイン
2. ヘッダーに「主催者」「イベント作成」ボタンが表示されることを確認
3. 「管理者」ボタンは**表示されない**ことを確認
4. URLで直接 `/admin` にアクセスしても権限エラーになることを確認
5. イベント作成・承認申請をテスト
6. ログアウト

### 3. 一般ユーザーテスト
1. `user@linkup.live` / `User@2026!` でログイン
2. ヘッダーに特別なボタンが**表示されない**ことを確認
3. URLで直接 `/organizer` にアクセスしても権限エラーになることを確認
4. URLで直接 `/admin` にアクセスしても権限エラーになることを確認
5. イベント検索・チケット購入をテスト
6. ログアウト

### 4. 既存ユーザーテスト
1. `iwama@inre.co.jp` / (既存パスワード) でログイン
2. 一般ユーザーと同じ権限になっていることを確認
3. オーガナイザー・管理者機能にアクセスできないことを確認

---

## 🔧 トラブルシューティング

### Q: ログインできない
**A**: 以下を確認してください：
1. メールアドレス・パスワードが正しいか
2. Cloudflare D1にマイグレーションが適用されているか
3. バックエンドがデプロイされているか
4. ブラウザのコンソールにエラーが出ていないか

### Q: 権限エラーが出る
**A**: 以下を確認してください：
1. 正しいアカウントでログインしているか
2. `store.user.role` が正しく設定されているか（ブラウザコンソールで確認）
3. バックエンドの権限ミドルウェアが正しく動作しているか

### Q: ボタンが表示されない
**A**: 以下を確認してください：
1. ログインしているか
2. `updateNav()` が呼ばれているか
3. ブラウザをハードリロード（Ctrl+Shift+R）

---

## 📊 権限マトリックス

| 機能 | admin | organizer | user |
|-----|-------|-----------|------|
| イベント閲覧 | ✅ | ✅ | ✅ |
| チケット購入 | ✅ | ✅ | ✅ |
| マイページ | ✅ | ✅ | ✅ |
| イベント作成 | ✅ | ✅ | ❌ |
| イベント編集（自分） | ✅ | ✅ | ❌ |
| イベント承認申請 | ✅ | ✅ | ❌ |
| 参加者管理 | ✅ | ✅ | ❌ |
| 売上確認 | ✅ | ✅ | ❌ |
| イベント承認・却下 | ✅ | ❌ | ❌ |
| ユーザー管理 | ✅ | ❌ | ❌ |
| システム設定 | ✅ | ❌ | ❌ |
| バックアップ管理 | ✅ | ❌ | ❌ |

---

**作成日時**: 2026-02-13 16:00 JST  
**バージョン**: v4.0.0-RBAC-SECURITY  
**最終更新**: テストユーザー登録完了後
