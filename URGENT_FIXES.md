# 🚨 緊急修正リスト

**作成日時**: 2026-02-13 11:00:00  
**優先度**: 最高  

---

## 📋 修正すべき問題

### 1. ⚠️ イベント保存後に一覧表示されない【最優先】

**原因**: バックエンドが古いバージョンのまま  
**解決策**: 最新バージョン（dd3c88e）をデプロイ  

**確認方法**:
```bash
curl https://linkup-backend.gcimaster.workers.dev/health
```

**期待される結果**: 200 OK

---

### 2. ❌ 承認申請機能が不明

**現状**: 
- 承認申請ボタンはある
- しかし承認フローが不明
- 管理者に承認画面があるか不明

**必要な実装**:
- [ ] 承認申請時に status='pending' で保存
- [ ] 管理者ダッシュボードに承認待ちイベント一覧
- [ ] 承認/却下ボタンの実装
- [ ] 承認後に status='published' に変更

---

### 3. ❌ プレビュー機能が動作しない

**現状**: プレビューボタンを押しても何も起きない

**必要な実装**:
- [ ] プレビューモーダルの実装
- [ ] イベントデータをプレビュー表示
- [ ] プレビューURLの生成

---

### 4. ❓ 保存先が不明

**質問**: 「保存ボタンはどこに保存している?」

**回答**:
- `POST /api/events` → D1 Database (events テーブル)
- しかし、バックエンドがデプロイされていないため実際には保存されていない可能性

---

### 5. 🆕 資料アップロード機能（未実装）

**要件**:
- イベント作成フォームに資料アップロード欄を追加
- PDF, DOCX, XLSX, PPT などのファイルをアップロード可能
- R2 バケットに保存

**実装方法**:
```javascript
// フロントエンド
<input type="file" id="event-document" accept=".pdf,.docx,.xlsx,.pptx" />

// バックエンド
POST /api/upload/document
→ Cloudflare R2 に保存
→ URL を返す
→ events テーブルに document_url カラムを追加
```

---

### 6. 🆕 資料ダウンロードボタン（未実装）

**要件**:
- イベント詳細ページに資料ダウンロードボタンを追加
- document_url が存在する場合のみ表示

**実装方法**:
```html
<a href="${event.document_url}" download class="btn-download">
  <span class="material-icons">download</span>
  資料をダウンロード
</a>
```

---

### 7. ⚠️ AI詳細作成のレベルが低い

**現状**: AI生成結果が期待に満たない

**確認項目**:
- [ ] Workers AI が実際に呼ばれているか
- [ ] プロンプトの内容は適切か
- [ ] モデルの選択は適切か

**改善方法**:
- プロンプトを詳細化
- より高性能なモデルに変更
- Few-shot learning を追加

---

## 🎯 修正の優先順位

### P0 (最優先 - 今すぐ)
1. バックエンドのデプロイ
2. イベント保存→一覧表示の動作確認

### P1 (高優先度 - 今日中)
3. 承認申請フローの完全実装
4. プレビュー機能の実装

### P2 (中優先度 - 明日まで)
5. 資料アップロード機能の追加
6. 資料ダウンロードボタンの追加

### P3 (通常優先度)
7. AI詳細作成の改善

---

## ✅ 実装計画

すべてを一括で実装します（推定時間: 2時間）

