# ─── APIプロキシ（CORSなし・同一ドメインで動作）─────────────
# link-up.live/api/* → Cloudflare Workers に転送
/api/* https://linkup-backend.gcimaster.workers.dev/api/:splat 200

# ─── SPA ルーティング ───────────────────────────────────────
/events/* /index.html 200
/organizers/* /index.html 200
/category/* /index.html 200
/pages/* /index.html 200
/dashboard /index.html 200
/organizer /index.html 200
/organizer/* /index.html 200
/admin /index.html 200

# ─── 静的ファイル ──────────────────────────────────────────
/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200
/assets/* /assets/:splat 200

# ─── フォールバック ────────────────────────────────────────
/* /index.html 200
